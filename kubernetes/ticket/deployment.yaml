apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-svc
  namespace: eventos-platform
  labels:
    app: ticket-svc
    service: ticket
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ticket-svc
  template:
    metadata:
      labels:
        app: ticket-svc
        service: ticket
    spec:
      containers:
        - name: ticket-svc
          image: ghcr.io/eventos-ai/ticket:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              value: "3003"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-svc
  namespace: eventos-platform
spec:
  selector:
    app: ticket-svc
  ports:
    - port: 3003
      targetPort: 3003
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ticket-svc
  namespace: eventos-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ticket-svc
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
