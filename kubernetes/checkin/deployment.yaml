apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkin-svc
  namespace: eventos-platform
  labels:
    app: checkin-svc
    service: checkin
spec:
  replicas: 3
  selector:
    matchLabels:
      app: checkin-svc
  template:
    metadata:
      labels:
        app: checkin-svc
        service: checkin
    spec:
      containers:
        - name: checkin-svc
          image: ghcr.io/eventos-ai/checkin:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
          env:
            - name: PORT
              value: "3004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: checkin-svc
  namespace: eventos-platform
spec:
  selector:
    app: checkin-svc
  ports:
    - port: 3004
      targetPort: 3004
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: checkin-svc
  namespace: eventos-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: checkin-svc
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
