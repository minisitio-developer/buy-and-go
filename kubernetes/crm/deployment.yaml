apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-svc
  namespace: eventos-platform
  labels:
    app: crm-svc
    service: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crm-svc
  template:
    metadata:
      labels:
        app: crm-svc
        service: crm
    spec:
      containers:
        - name: crm-svc
          image: ghcr.io/eventos-ai/crm:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3005
          env:
            - name: PORT
              value: "3005"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: crm-svc
  namespace: eventos-platform
spec:
  selector:
    app: crm-svc
  ports:
    - port: 3005
      targetPort: 3005
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: crm-svc
  namespace: eventos-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: crm-svc
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
