apiVersion: apps/v1
kind: Deployment
metadata:
  name: sponsor-svc
  namespace: eventos-platform
  labels:
    app: sponsor-svc
    service: sponsor
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sponsor-svc
  template:
    metadata:
      labels:
        app: sponsor-svc
        service: sponsor
    spec:
      containers:
        - name: sponsor-svc
          image: ghcr.io/eventos-ai/sponsor:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
          env:
            - name: PORT
              value: "3006"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: eventos-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: sponsor-svc
  namespace: eventos-platform
spec:
  selector:
    app: sponsor-svc
  ports:
    - port: 3006
      targetPort: 3006
      name: http
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sponsor-svc
  namespace: eventos-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sponsor-svc
  minReplicas: 3
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
